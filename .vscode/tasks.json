{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Clean and start dev server",
			"type": "shell",
			"command": "rm -rf .next && pnpm install && pnpm run dev",
			"isBackground": false,
			"problemMatcher": [
				"$tsc"
			],
			"group": "build"
		}
	]
}
"use client";

import { EmailVerificationPage } from "../../components/EmailVerificationPage";

export default function Page() {
  // Next.js route for /verifikasi-email
  return <EmailVerificationPage onNavigate={() => {}} />;
}
